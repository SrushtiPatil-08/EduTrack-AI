export async function startScan() {
  return { error: 'Scanner service not configured yet.' };
}

export async function processScanResult(data) {
  return { error: 'Scanner service not configured yet.' };
}

export default { startScan, processScanResult };
